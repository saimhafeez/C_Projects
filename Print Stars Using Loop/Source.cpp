#include <stdio.h>
#include <stdlib.h>
int main() {
	int stars = 0, spaces = 0, count=1;

	for (count <= 1; count <= 5; count++)
	{
		for (spaces=5; spaces >=0; spaces--)

		printf("*");

	}
	system("pause");
	return 0;
}